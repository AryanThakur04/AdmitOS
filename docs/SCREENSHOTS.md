# Analytics Screenshots (Demo)

When running the dashboard, capture these views for your portfolio:

1. **Command Dashboard** — `/dashboard` — KPI cards + weekly chart + source pie
2. **Lead Pipeline** — `/leads` — Tier badges + aging bars
3. **Alert Center** — `/alerts` — Risk notifications
4. **Admin Analytics** — `/analytics` — Lost reasons + counsellor table + doc heatmap
5. **AI Tools** — `/tools` — WhatsApp simulator + acceptance pack
6. **Landing Page** — `/` — Product hero + feature grid

## Sample KPIs (seeded data)

| Metric | Value |
|--------|-------|
| Total Leads | 15 |
| Hot / Medium / Cold | ~5 / 6 / 4 |
| Active Alerts | 8+ |
| Conversion Rate | ~13% |

Save screenshots to `docs/screenshots/` for README embedding.
